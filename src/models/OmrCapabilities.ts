/* tslint:disable */
/* eslint-disable */
/**
 * Flat API
 * The Flat API allows you to easily extend the abilities of the [Flat Platform](https://flat.io), with a wide range of use cases including the following:  * Creating and importing new music scores using MusicXML, MIDI, Guitar Pro, MuseScore, ABC notation, and many other formats * Browsing, updating, copying, exporting the user\'s scores (for example in MP3, WAV or MIDI) * Managing educational resources with Flat for Education: creating & updating the organization accounts, the classes, rosters and assignments.  The Flat API is built on HTTP. Our API is RESTful It has predictable resource URLs. It returns HTTP response codes to indicate errors. It also accepts and returns JSON in the HTTP body. The [schema](/swagger.yaml) of this API follows the [OpenAPI Initiative (OAI) specification](https://www.openapis.org/), you can use and work with [compatible Swagger tools](http://swagger.io/open-source-integrations/). This API features Cross-Origin Resource Sharing (CORS) implemented in compliance with [W3C spec](https://www.w3.org/TR/cors/).  You can use your favorite HTTP/REST library for your programming language to use Flat\'s API. This specification and reference is [available on Github](https://github.com/FlatIO/api-reference).  Getting Started and learn more:  * [API Overview and introduction](https://flat.io/developers/docs/api/) * [Authentication (Personal Access Tokens or OAuth2)](https://flat.io/developers/docs/api/authentication.html) * [SDKs](https://flat.io/developers/docs/api/sdks.html) * [Rate Limits](https://flat.io/developers/docs/api/rate-limits.html) * [Changelog](https://flat.io/developers/docs/api/changelog.html) 
 *
 * The version of the OpenAPI document: 2.26.1
 * Contact: developers@flat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { OmrJobOutput } from './OmrJobOutput.js';
import {
    OmrJobOutputFromJSON,
    OmrJobOutputFromJSONTyped,
    OmrJobOutputToJSON,
    OmrJobOutputToJSONTyped,
} from './OmrJobOutput.js';
import type { OmrStepName } from './OmrStepName.js';
import {
    OmrStepNameFromJSON,
    OmrStepNameFromJSONTyped,
    OmrStepNameToJSON,
    OmrStepNameToJSONTyped,
} from './OmrStepName.js';
import type { OmrLocaleDetails } from './OmrLocaleDetails.js';
import {
    OmrLocaleDetailsFromJSON,
    OmrLocaleDetailsFromJSONTyped,
    OmrLocaleDetailsToJSON,
    OmrLocaleDetailsToJSONTyped,
} from './OmrLocaleDetails.js';

/**
 * What the account or app can do, for client feature-detection.
 * @export
 * @interface OmrCapabilities
 */
export interface OmrCapabilities {
    /**
     * Interactive steps this server supports.
     * @type {Array<OmrStepName>}
     * @memberof OmrCapabilities
     */
    steps: Array<OmrStepName>;
    /**
     * Export formats available via `getOmrJobExport`.
     * @type {Array<string>}
     * @memberof OmrCapabilities
     */
    formats: Array<string>;
    /**
     * Output destinations the account can use when creating a job.
     * @type {Array<OmrJobOutput>}
     * @memberof OmrCapabilities
     */
    outputs: Array<OmrJobOutput>;
    /**
     * Maximum number of input files that can be added to a single job.
     * @type {number}
     * @memberof OmrCapabilities
     */
    maxFiles: number;
    /**
     * Maximum number of pages allowed across all input files of a single job.
     * @type {number}
     * @memberof OmrCapabilities
     */
    maxPages: number;
    /**
     * Maximum number of OMR jobs that can run in parallel for this account.
     * @type {number}
     * @memberof OmrCapabilities
     */
    maxParallelJobs: number;
    /**
     * Maximum size of a single file, in bytes.
     * @type {number}
     * @memberof OmrCapabilities
     */
    maxFileSize: number;
    /**
     * MIME types accepted for input files: PDF, plus the raster image formats.
     * 
     * Drive the file picker from this list rather than hardcoding it, so newly
     * supported formats need no client release. A file is identified by its content,
     * so its declared type and its extension do not have to match.
     * 
     * A multi-page input counts as several pages against `maxPages` and is charged
     * accordingly. That covers PDFs and, among the image formats, multi-page TIFF and
     * animated GIF/WebP.
     * 
     * @type {Array<string>}
     * @memberof OmrCapabilities
     */
    acceptedMimeTypes: Array<string>;
    /**
     * Filename extensions the accepted types appear under, for building a file picker.
     * 
     * Use these alongside `acceptedMimeTypes` in an `accept` attribute: browsers and native
     * file dialogs filter unreliably on some of the image types, so a valid file can be
     * greyed out when only its MIME type is offered.
     * 
     * Longer than `acceptedMimeTypes`, because one type arrives under several extensions
     * (`.jpg` and `.jpeg`, `.tif` and `.tiff`, `.heic` and `.heif`).
     * 
     * Picker metadata only. A file is identified by its content, so its extension never
     * decides whether an upload is accepted.
     * 
     * @type {Array<string>}
     * @memberof OmrCapabilities
     */
    acceptedExtensions: Array<string>;
    /**
     * Credits charged per page.
     * @type {number}
     * @memberof OmrCapabilities
     */
    costPerPage?: number;
    /**
     * OMR credits remaining for the account.
     * @type {number}
     * @memberof OmrCapabilities
     */
    remainingCredits?: number;
    /**
     * How many days a `musicxml` job's uploaded files and results are kept before erasure.
     * 
     * Reflects the account's own period when one has been set, otherwise the platform
     * default. Read-only: contact support to change it. Jobs with `output: library` are
     * not covered by the retention policy and are unaffected by this value.
     * 
     * @type {number}
     * @memberof OmrCapabilities
     */
    retentionDays?: number;
    /**
     * Locales selectable for OCR, as BCP 47 codes sorted alphabetically.
     * 
     * These are the locales the recognition pipeline can actually read, which is neither the
     * list of Flat interface locales nor a fixed set: new languages are added over time.
     * Clients should default to the user's own locale when it appears here.
     * 
     * @type {Array<string>}
     * @memberof OmrCapabilities
     */
    locales: Array<string>;
    /**
     * The same locales as `locales`, each with its English display name, sorted alphabetically
     * by `name` and ready to bind to a language picker.
     * 
     * Prefer this over `locales` when rendering a selector: it saves clients from shipping
     * their own code-to-label table.
     * 
     * @type {Array<OmrLocaleDetails>}
     * @memberof OmrCapabilities
     */
    localesDetails: Array<OmrLocaleDetails>;
}

/**
 * Check if a given object implements the OmrCapabilities interface.
 */
export function instanceOfOmrCapabilities(value: object): value is OmrCapabilities {
    if (!('steps' in value) || value['steps'] === undefined) return false;
    if (!('formats' in value) || value['formats'] === undefined) return false;
    if (!('outputs' in value) || value['outputs'] === undefined) return false;
    if (!('maxFiles' in value) || value['maxFiles'] === undefined) return false;
    if (!('maxPages' in value) || value['maxPages'] === undefined) return false;
    if (!('maxParallelJobs' in value) || value['maxParallelJobs'] === undefined) return false;
    if (!('maxFileSize' in value) || value['maxFileSize'] === undefined) return false;
    if (!('acceptedMimeTypes' in value) || value['acceptedMimeTypes'] === undefined) return false;
    if (!('acceptedExtensions' in value) || value['acceptedExtensions'] === undefined) return false;
    if (!('locales' in value) || value['locales'] === undefined) return false;
    if (!('localesDetails' in value) || value['localesDetails'] === undefined) return false;
    return true;
}

export function OmrCapabilitiesFromJSON(json: any): OmrCapabilities {
    return OmrCapabilitiesFromJSONTyped(json, false);
}

export function OmrCapabilitiesFromJSONTyped(json: any, ignoreDiscriminator: boolean): OmrCapabilities {
    if (json == null) {
        return json;
    }
    return {
        
        'steps': ((json['steps'] as Array<any>).map(OmrStepNameFromJSON)),
        'formats': json['formats'],
        'outputs': ((json['outputs'] as Array<any>).map(OmrJobOutputFromJSON)),
        'maxFiles': json['maxFiles'],
        'maxPages': json['maxPages'],
        'maxParallelJobs': json['maxParallelJobs'],
        'maxFileSize': json['maxFileSize'],
        'acceptedMimeTypes': json['acceptedMimeTypes'],
        'acceptedExtensions': json['acceptedExtensions'],
        'costPerPage': json['costPerPage'] == null ? undefined : json['costPerPage'],
        'remainingCredits': json['remainingCredits'] == null ? undefined : json['remainingCredits'],
        'retentionDays': json['retentionDays'] == null ? undefined : json['retentionDays'],
        'locales': json['locales'],
        'localesDetails': ((json['localesDetails'] as Array<any>).map(OmrLocaleDetailsFromJSON)),
    };
}

export function OmrCapabilitiesToJSON(json: any): OmrCapabilities {
    return OmrCapabilitiesToJSONTyped(json, false);
}

export function OmrCapabilitiesToJSONTyped(value?: OmrCapabilities | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'steps': ((value['steps'] as Array<any>).map(OmrStepNameToJSON)),
        'formats': value['formats'],
        'outputs': ((value['outputs'] as Array<any>).map(OmrJobOutputToJSON)),
        'maxFiles': value['maxFiles'],
        'maxPages': value['maxPages'],
        'maxParallelJobs': value['maxParallelJobs'],
        'maxFileSize': value['maxFileSize'],
        'acceptedMimeTypes': value['acceptedMimeTypes'],
        'acceptedExtensions': value['acceptedExtensions'],
        'costPerPage': value['costPerPage'],
        'remainingCredits': value['remainingCredits'],
        'retentionDays': value['retentionDays'],
        'locales': value['locales'],
        'localesDetails': ((value['localesDetails'] as Array<any>).map(OmrLocaleDetailsToJSON)),
    };
}

