/* tslint:disable */
/* eslint-disable */
/**
 * Flat API
 * The Flat API allows you to easily extend the abilities of the [Flat Platform](https://flat.io), with a wide range of use cases including the following:  * Creating and importing new music scores using MusicXML, MIDI, Guitar Pro, MuseScore, ABC notation, and many other formats * Browsing, updating, copying, exporting the user\'s scores (for example in MP3, WAV or MIDI) * Managing educational resources with Flat for Education: creating & updating the organization accounts, the classes, rosters and assignments.  The Flat API is built on HTTP. Our API is RESTful It has predictable resource URLs. It returns HTTP response codes to indicate errors. It also accepts and returns JSON in the HTTP body. The [schema](/swagger.yaml) of this API follows the [OpenAPI Initiative (OAI) specification](https://www.openapis.org/), you can use and work with [compatible Swagger tools](http://swagger.io/open-source-integrations/). This API features Cross-Origin Resource Sharing (CORS) implemented in compliance with [W3C spec](https://www.w3.org/TR/cors/).  You can use your favorite HTTP/REST library for your programming language to use Flat\'s API. This specification and reference is [available on Github](https://github.com/FlatIO/api-reference).  Getting Started and learn more:  * [API Overview and introduction](https://flat.io/developers/docs/api/) * [Authentication (Personal Access Tokens or OAuth2)](https://flat.io/developers/docs/api/authentication.html) * [SDKs](https://flat.io/developers/docs/api/sdks.html) * [Rate Limits](https://flat.io/developers/docs/api/rate-limits.html) * [Changelog](https://flat.io/developers/docs/api/changelog.html) 
 *
 * The version of the OpenAPI document: 2.26.1
 * Contact: developers@flat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * Override for a single detected part, matched by `index`. Omitted fields keep the
 * detected value.
 * 
 * To set the instrument you may use **either** `instrumentId` (Flat's instrument id)
 * **or** `midiProgram` (a standard General MIDI program) — whichever your integration
 * prefers; you do not need both. If both are sent, `instrumentId` wins; otherwise
 * `midiProgram` is resolved to the matching instrument; otherwise the detected
 * instrument is kept. `transposeKey` and `partName` apply independently on top.
 * 
 * @export
 * @interface OmrInstrumentOverride
 */
export interface OmrInstrumentOverride {
    /**
     * 0-based position of the part to override, matching the `index` of the detected part.
     * @type {number}
     * @memberof OmrInstrumentOverride
     */
    index: number;
    /**
     * Override the resolved instrument with this Flat instrument id, for example
     * `brass.horn`. See the [Instrument IDs reference](https://flat.io/developers/docs/api/instruments)
     * for valid values. Both the dotted `<group>.<instrument>` form (`brass.horn`) and the bare
     * instrument key (`horn`) are accepted. Use this or `midiProgram`. Takes precedence over
     * `midiProgram` when both are set.
     * 
     * @type {string}
     * @memberof OmrInstrumentOverride
     */
    instrumentId?: string;
    /**
     * Override the part name.
     * @type {string}
     * @memberof OmrInstrumentOverride
     */
    partName?: string;
    /**
     * Override the transposition / written key: a pitch class as a letter `A`-`G` with an
     * optional accidental. For example `F` for Horn in F or `Bb` for a B flat clarinet.
     * 
     * The accidental may be ASCII `b` (flat) or `#` (sharp), or the Unicode music glyphs
     * `♭` (U+266D) and `♯` (U+266F). Unicode accidentals are normalized to their ASCII
     * equivalent, so `B♭` is stored and returned as `Bb`.
     * 
     * @type {string}
     * @memberof OmrInstrumentOverride
     */
    transposeKey?: string;
    /**
     * Override the instrument with a standard General MIDI program number (0-127),
     * resolved server-side to the matching Flat instrument. Use this when you do not want
     * to map Flat instrument ids. Ignored if `instrumentId` is also set.
     * 
     * @type {number}
     * @memberof OmrInstrumentOverride
     */
    midiProgram?: number;
}

/**
 * Check if a given object implements the OmrInstrumentOverride interface.
 */
export function instanceOfOmrInstrumentOverride(value: object): value is OmrInstrumentOverride {
    if (!('index' in value) || value['index'] === undefined) return false;
    return true;
}

export function OmrInstrumentOverrideFromJSON(json: any): OmrInstrumentOverride {
    return OmrInstrumentOverrideFromJSONTyped(json, false);
}

export function OmrInstrumentOverrideFromJSONTyped(json: any, ignoreDiscriminator: boolean): OmrInstrumentOverride {
    if (json == null) {
        return json;
    }
    return {
        
        'index': json['index'],
        'instrumentId': json['instrumentId'] == null ? undefined : json['instrumentId'],
        'partName': json['partName'] == null ? undefined : json['partName'],
        'transposeKey': json['transposeKey'] == null ? undefined : json['transposeKey'],
        'midiProgram': json['midiProgram'] == null ? undefined : json['midiProgram'],
    };
}

export function OmrInstrumentOverrideToJSON(json: any): OmrInstrumentOverride {
    return OmrInstrumentOverrideToJSONTyped(json, false);
}

export function OmrInstrumentOverrideToJSONTyped(value?: OmrInstrumentOverride | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'index': value['index'],
        'instrumentId': value['instrumentId'],
        'partName': value['partName'],
        'transposeKey': value['transposeKey'],
        'midiProgram': value['midiProgram'],
    };
}

