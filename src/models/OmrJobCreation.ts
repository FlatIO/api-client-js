/* tslint:disable */
/* eslint-disable */
/**
 * Flat API
 * The Flat API allows you to easily extend the abilities of the [Flat Platform](https://flat.io), with a wide range of use cases including the following:  * Creating and importing new music scores using MusicXML, MIDI, Guitar Pro, MuseScore, ABC notation, and many other formats * Browsing, updating, copying, exporting the user\'s scores (for example in MP3, WAV or MIDI) * Managing educational resources with Flat for Education: creating & updating the organization accounts, the classes, rosters and assignments.  The Flat API is built on HTTP. Our API is RESTful: it has predictable resource URLs. It returns HTTP response codes to indicate errors. It also accepts and returns JSON in the HTTP body. The [schema](/swagger.yaml) of this API follows the [OpenAPI Initiative (OAI) specification](https://www.openapis.org/), you can use and work with [compatible Swagger tools](http://swagger.io/open-source-integrations/). This API features Cross-Origin Resource Sharing (CORS) implemented in compliance with [W3C spec](https://www.w3.org/TR/cors/).  You can use your favorite HTTP/REST library for your programming language to use Flat\'s API. This specification and reference are [available on GitHub](https://github.com/FlatIO/api-reference).  Getting Started and learn more:  * [API Overview and introduction](https://flat.io/developers/docs/api/) * [Authentication (Personal Access Tokens or OAuth2)](https://flat.io/developers/docs/api/authentication.html) * [SDKs](https://flat.io/developers/docs/api/sdks.html) * [Rate Limits](https://flat.io/developers/docs/api/rate-limits.html) * [Changelog](https://flat.io/developers/docs/api/changelog.html) 
 *
 * The version of the OpenAPI document: 2.26.2
 * Contact: developers@flat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { OmrJobOutput } from './OmrJobOutput.js';
import {
    OmrJobOutputFromJSON,
    OmrJobOutputFromJSONTyped,
    OmrJobOutputToJSON,
    OmrJobOutputToJSONTyped,
} from './OmrJobOutput.js';
import type { OmrJobInputFile } from './OmrJobInputFile.js';
import {
    OmrJobInputFileFromJSON,
    OmrJobInputFileFromJSONTyped,
    OmrJobInputFileToJSON,
    OmrJobInputFileToJSONTyped,
} from './OmrJobInputFile.js';
import type { OmrStepName } from './OmrStepName.js';
import {
    OmrStepNameFromJSON,
    OmrStepNameFromJSONTyped,
    OmrStepNameToJSON,
    OmrStepNameToJSONTyped,
} from './OmrStepName.js';

/**
 * Parameters to create an OMR job. Send without `files` to create a draft (then add
 * files with `addOmrJobFile` and run `startOmrJob`), or include `files` and
 * `autoStart: true` to import in a single request.
 * 
 * @export
 * @interface OmrJobCreation
 */
export interface OmrJobCreation {
    /**
     * 
     * @type {OmrJobOutput}
     * @memberof OmrJobCreation
     */
    output?: OmrJobOutput;
    /**
     * Steps at which the pipeline should pause for this client. Omit or send `[]` for a
     * fully automatic import. The server only pauses at the steps listed here; declare
     * only steps your client can actually render.
     * 
     * @type {Array<OmrStepName>}
     * @memberof OmrJobCreation
     */
    interactiveSteps?: Array<OmrStepName>;
    /**
     * Let the server detect and correct pages uploaded sideways or upside down
     * (90/180/270°) before recognition. Off by default: pages are recognized in the
     * orientation they are uploaded in, which is the right choice when your client lets
     * the user rotate pages before upload. Enable it when it does not, for example a
     * server-to-server integration importing scans as they come. Small tilt correction
     * (a few degrees) always runs.
     * 
     * @type {boolean}
     * @memberof OmrJobCreation
     */
    autoRotate?: boolean;
    /**
     * Locale hints (BCP 47) to improve text and lyric detection, for example
     * `["ja", "en"]`. The first entry is the primary reading language. This is the input
     * hint; the main language can be confirmed or corrected at the `details` step.
     * 
     * @type {Array<string>}
     * @memberof OmrJobCreation
     */
    locales?: Array<string>;
    /**
     * Target collection ID. Only used when `output` is `library`.
     * @type {string}
     * @memberof OmrJobCreation
     */
    collection?: string;
    /**
     * Optional client-supplied key. A retry with the same key returns the existing job
     * instead of creating a duplicate, for safe retries on flaky networks.
     * 
     * @type {string}
     * @memberof OmrJobCreation
     */
    idempotencyKey?: string;
    /**
     * Optional inline inputs for a one-shot import. For multi-image or mobile capture, omit this and use `addOmrJobFile`.
     * @type {Array<OmrJobInputFile>}
     * @memberof OmrJobCreation
     */
    files?: Array<OmrJobInputFile>;
    /**
     * Start processing immediately. Only valid when `files` is provided.
     * @type {boolean}
     * @memberof OmrJobCreation
     */
    autoStart?: boolean;
}



/**
 * Check if a given object implements the OmrJobCreation interface.
 */
export function instanceOfOmrJobCreation(value: object): value is OmrJobCreation {
    return true;
}

export function OmrJobCreationFromJSON(json: any): OmrJobCreation {
    return OmrJobCreationFromJSONTyped(json, false);
}

export function OmrJobCreationFromJSONTyped(json: any, ignoreDiscriminator: boolean): OmrJobCreation {
    if (json == null) {
        return json;
    }
    return {
        
        'output': json['output'] == null ? undefined : OmrJobOutputFromJSON(json['output']),
        'interactiveSteps': json['interactiveSteps'] == null ? undefined : ((json['interactiveSteps'] as Array<any>).map(OmrStepNameFromJSON)),
        'autoRotate': json['autoRotate'] == null ? undefined : json['autoRotate'],
        'locales': json['locales'] == null ? undefined : json['locales'],
        'collection': json['collection'] == null ? undefined : json['collection'],
        'idempotencyKey': json['idempotencyKey'] == null ? undefined : json['idempotencyKey'],
        'files': json['files'] == null ? undefined : ((json['files'] as Array<any>).map(OmrJobInputFileFromJSON)),
        'autoStart': json['autoStart'] == null ? undefined : json['autoStart'],
    };
}

export function OmrJobCreationToJSON(json: any): OmrJobCreation {
    return OmrJobCreationToJSONTyped(json, false);
}

export function OmrJobCreationToJSONTyped(value?: OmrJobCreation | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'output': OmrJobOutputToJSON(value['output']),
        'interactiveSteps': value['interactiveSteps'] == null ? undefined : ((value['interactiveSteps'] as Array<any>).map(OmrStepNameToJSON)),
        'autoRotate': value['autoRotate'],
        'locales': value['locales'],
        'collection': value['collection'],
        'idempotencyKey': value['idempotencyKey'],
        'files': value['files'] == null ? undefined : ((value['files'] as Array<any>).map(OmrJobInputFileToJSON)),
        'autoStart': value['autoStart'],
    };
}

