/* tslint:disable */
/* eslint-disable */
/**
 * Flat API
 * The Flat API allows you to easily extend the abilities of the [Flat Platform](https://flat.io), with a wide range of use cases including the following:  * Creating and importing new music scores using MusicXML, MIDI, Guitar Pro, MuseScore, ABC notation, and many other formats * Browsing, updating, copying, exporting the user\'s scores (for example in MP3, WAV or MIDI) * Managing educational resources with Flat for Education: creating & updating the organization accounts, the classes, rosters and assignments.  The Flat API is built on HTTP. Our API is RESTful: it has predictable resource URLs. It returns HTTP response codes to indicate errors. It also accepts and returns JSON in the HTTP body. The [schema](/swagger.yaml) of this API follows the [OpenAPI Initiative (OAI) specification](https://www.openapis.org/), you can use and work with [compatible Swagger tools](http://swagger.io/open-source-integrations/). This API features Cross-Origin Resource Sharing (CORS) implemented in compliance with [W3C spec](https://www.w3.org/TR/cors/).  You can use your favorite HTTP/REST library for your programming language to use Flat\'s API. This specification and reference are [available on GitHub](https://github.com/FlatIO/api-reference).  Getting Started and learn more:  * [API Overview and introduction](https://flat.io/developers/docs/api/) * [Authentication (Personal Access Tokens or OAuth2)](https://flat.io/developers/docs/api/authentication.html) * [SDKs](https://flat.io/developers/docs/api/sdks.html) * [Rate Limits](https://flat.io/developers/docs/api/rate-limits.html) * [Changelog](https://flat.io/developers/docs/api/changelog.html) 
 *
 * The version of the OpenAPI document: 2.26.2
 * Contact: developers@flat.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * Data-retention state of the job.
 * 
 * Present only on jobs whose `output` is `musicxml`. Jobs imported into the Flat library
 * are part of your library content, are not covered by this policy, and omit this object
 * entirely.
 * 
 * Erasure happens shortly after `expiryDate` rather than exactly on it. Plan for the
 * deadline, not the instant.
 * 
 * @export
 * @interface OmrJobRetention
 */
export interface OmrJobRetention {
    /**
     * When this job's uploaded files and recognition results become eligible for erasure.
     * Fixed when the job is created: changing the account's retention period does not move
     * the deadline of jobs that already exist.
     * 
     * @type {Date}
     * @memberof OmrJobRetention
     */
    expiryDate: Date;
    /**
     * When the job's stored files were actually erased. Present only once that happened.
     * 
     * An expired job keeps the `status` it finished with and stays listable, but its
     * `result` is no longer served and downloads fail with `OMR_JOB_EXPIRED`.
     * 
     * @type {Date}
     * @memberof OmrJobRetention
     */
    expiredDate?: Date;
}

/**
 * Check if a given object implements the OmrJobRetention interface.
 */
export function instanceOfOmrJobRetention(value: object): value is OmrJobRetention {
    if (!('expiryDate' in value) || value['expiryDate'] === undefined) return false;
    return true;
}

export function OmrJobRetentionFromJSON(json: any): OmrJobRetention {
    return OmrJobRetentionFromJSONTyped(json, false);
}

export function OmrJobRetentionFromJSONTyped(json: any, ignoreDiscriminator: boolean): OmrJobRetention {
    if (json == null) {
        return json;
    }
    return {
        
        'expiryDate': (new Date(json['expiryDate'])),
        'expiredDate': json['expiredDate'] == null ? undefined : (new Date(json['expiredDate'])),
    };
}

export function OmrJobRetentionToJSON(json: any): OmrJobRetention {
    return OmrJobRetentionToJSONTyped(json, false);
}

export function OmrJobRetentionToJSONTyped(value?: OmrJobRetention | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'expiryDate': value['expiryDate'].toISOString(),
        'expiredDate': value['expiredDate'] == null ? value['expiredDate'] : value['expiredDate'].toISOString(),
    };
}

